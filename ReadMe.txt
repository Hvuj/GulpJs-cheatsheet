# install gulp

# npm install gulp-cli -g
# npm install gulp -D
# npx -p touch nodetouch gulpfile.js
# gulp --help

# npm install --save-dev gulp
# gulp --version

# install sass and uglify

# npm install node-sass gulp-sass --save-dev
# npm install --save gulp-uglifycss

# CONFIG gulpfile.js

# create gulp task for auto run for all changes

# npm install --save-dev gulp-rename

# install auto prefixer

# npm install --save-dev gulp-autoprefixer

# npm install --save-dev gulp-sourcemaps

# install browserify

# npm install --save-dev browserify 

# install babelify

# npm install --save-dev babelify

npm install --save-dev @babel/core @babel/cli @babel/preset-env
npm install --save @babel/polyfill
# npm install --save-dev babel-core

# npm install --save-dev babel-preset-env

# npm install --save-dev vinyl-source-stream

# npm install --save-dev vinyl-buffer

#uglify(minifiy js)
# npm install gulp-terser --save-dev

# install browser sync

# npm install --save-dev browser-sync

# add reload to browser sync

# npm outdated  ---- to check what package is outdated

# npm install check-outdated --save-dev

# add to scripts in package.json 
"check-outdated": "check-outdated --ignore-pre-releases --ignore-dev-dependencies --ignore-packages package1,package2",
"preversion": "npm run lint && npm run test && npm run check-outdated"

# npm install --save-dev gulp-plumber

# npm install --save-dev gulp-notify

